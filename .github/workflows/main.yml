name: Daily update
on:
  schedule:
    - cron: "30 17 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Update data
      env: 
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        git submodule update --recursive --remote
        python -m pip install --upgrade pip
        pip install Flask
        python ./src/main.py > docs/index.html
        chmod +x ./src/update.sh
        ./src/update.sh
